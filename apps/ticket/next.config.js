/* eslint-disable no-undef */
// eslint-disable-next-line no-undef
const withTM = require('next-transpile-modules')(['@dudoong/ui']);
module.exports = withTM({
  // Any additional config for next goes in here
});
